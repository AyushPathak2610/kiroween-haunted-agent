{
  "mcpServers": {
    "blockchain-vows": {
      "command": "node",
      "args": ["mcp-servers/blockchain-vows-server.js"],
      "description": "Blockchain vow verification ledger - checks if Theo, Selene, and other ghosts kept their promises",
      "disabled": false,
      "autoApprove": ["check_vow", "record_vow", "list_all_vows"]
    },
    "replicate-image-gen": {
      "command": "uvx",
      "args": ["mcp-replicate"],
      "env": {
        "REPLICATE_API_TOKEN": "${REPLICATE_API_KEY}"
      },
      "description": "Mira's crayon drawing generator - creates childlike art when happy",
      "disabled": true,
      "autoApprove": ["run_model"]
    },
    "pinecone-memory": {
      "command": "uvx",
      "args": ["mcp-pinecone"],
      "env": {
        "PINECONE_API_KEY": "${PINECONE_API_KEY}"
      },
      "description": "Harlan's vector memory storage - remembers puzzle solutions",
      "disabled": true,
      "autoApprove": ["query", "upsert"]
    }
  }
}
